package machination.webjava.trees.scheme;

public interface ReplaceListener {

	/**
	 * Called when an Object has been replaced in a Scheme tree.
	 * 
	 * @param old
	 * @param n
	 */
	public void gotReplace(SchemeObject old, SchemeObject n);
}
